version: 2
general:
  artifacts:
    - "./tests/screenshots" #Save the screenshots for the tests
    
## Customize dependencies
dependencies:
  pre:
    - sudo pip install -U selenium	
 
## Running Tests
test:
   override:
    - python StravaLogin.py